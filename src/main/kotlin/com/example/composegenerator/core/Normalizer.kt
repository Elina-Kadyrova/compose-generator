package com.example.composegenerator.core

import com.example.composegenerator.model.util.Composable

class Normalizer(private val elements: MutableList<Composable>) {
    // TODO add normal normalize() implementation
    fun normalize(): MutableList<Composable> = elements
}